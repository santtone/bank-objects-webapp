bankObjects.Customer = function (id, firstName, lastName, address, bankId) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.address = address;
    this.bankId = bankId;
};